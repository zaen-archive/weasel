# Use ld.lld if the version is same
build/tools/compiler/compiler runtime-rt/lexer/main.we
# clang runtime-rt/main.o -o runtime-rt/a.out -Lbuild/lib/parallel  -lWeaselParallel -lOpenCL -lstdc++

echo ""
echo "--- Already Builded ---"
echo ""
